# Application Flow - Nova Storyteller

## User Journey

### 1. Registration Flow

```
┌──────────────┐
│ Landing Page │
└──────┬───────┘
       │
       │ Click "Sign Up"
       ▼
┌──────────────────┐
│ Registration Form│
│ - Email          │
│ - Username       │
│ - Password       │
│ - First Name     │
│ - Last Name      │
└──────┬───────────┘
       │
       │ Submit
       ▼
┌──────────────────┐
│ POST /api/       │
│ register/        │
└──────┬───────────┘
       │
       │ Success
       ▼
┌──────────────────┐
│ Auto Login       │
│ Redirect to      │
│ Dashboard        │
└──────────────────┘
```

### 2. Login Flow

```
┌──────────────┐
│ Login Page   │
└──────┬───────┘
       │
       │ Enter credentials
       ▼
┌──────────────────┐
│ POST /api/login/ │
└──────┬───────────┘
       │
       │ Success → Token
       ▼
┌──────────────────┐
│ Store Token      │
│ (localStorage)   │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Dashboard        │
│ (My Stories)     │
└──────────────────┘
```

### 3. Story Creation Flow

```
┌──────────────────┐
│ Dashboard        │
│ "My Stories"     │
└──────┬───────────┘
       │
       │ Click "Create Story"
       ▼
┌──────────────────────────────┐
│ Story Creation Page          │
│ ┌──────────────────────────┐ │
│ │ 1. Choose Template       │ │
│ │    [Adventure] [Fantasy] │ │
│ │    [Sci-Fi] [Mystery]    │ │
│ │    [Educational]         │ │
│ └──────────────────────────┘ │
│ ┌──────────────────────────┐ │
│ │ 2. Enter Story Prompt    │ │
│ │    [Text Input]          │ │
│ │    [🎤 Voice Input]      │ │
│ └──────────────────────────┘ │
│ ┌──────────────────────────┐ │
│ │ 3. Upload Image (Optional)│ │
│ │    [📷 Upload]           │ │
│ └──────────────────────────┘ │
│ ┌──────────────────────────┐ │
│ │ 4. [Create Story] Button │ │
│ └──────────────────────────┘ │
└──────┬───────────────────────┘
       │
       │ Submit
       ▼
┌──────────────────────────────┐
│ POST /api/stories/           │
│ {                            │
│   "title": "...",            │
│   "prompt": "...",           │
│   "template": "adventure",    │
│   "image": file (optional)   │
│ }                            │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Backend Processing           │
│ 1. Save story to DB          │
│ 2. If image: Analyze image   │
│    (Nova Titan)              │
│ 3. Generate story text       │
│    (Nova 2 Lite)             │
│ 4. Generate audio            │
│    (Nova 2 Sonic)            │
│ 5. Save audio file           │
│ 6. Update story record       │
└──────┬───────────────────────┘
       │
       │ Response with story data
       ▼
┌──────────────────────────────┐
│ Story Player Page            │
│ ┌──────────────────────────┐ │
│ │ Story Title              │ │
│ │                          │ │
│ │ [▶️ Play] [⏸️ Pause]     │ │
│ │                          │ │
│ │ Story Text Display       │ │
│ │ (scrollable)             │ │
│ │                          │ │
│ │ [Edit] [Save] [Delete]   │ │
│ └──────────────────────────┘ │
└──────────────────────────────┘
```

### 4. Story Listening Flow

```
┌──────────────────┐
│ Story Player     │
└──────┬───────────┘
       │
       │ Click "Play"
       ▼
┌──────────────────────────────┐
│ Audio Playback               │
│ - Stream from audio_file     │
│ - Highlight text as it plays │
│ - Show progress bar          │
└──────┬───────────────────────┘
       │
       │ User can:
       │ - Pause/Resume
       │ - Seek to position
       │ - Adjust volume
       │ - Interrupt with changes
       ▼
┌──────────────────────────────┐
│ Interrupt Story              │
│ "Make the astronaut have     │
│  a pet robot!"               │
└──────┬───────────────────────┘
       │
       │ POST /api/stories/{id}/regenerate/
       ▼
┌──────────────────────────────┐
│ Regenerate Story             │
│ (Nova 2 Lite with            │
│  modifications)              │
└──────┬───────────────────────┘
       │
       │ New story + audio
       ▼
┌──────────────────────────────┐
│ Updated Story Player         │
│ (with new content)           │
└──────────────────────────────┘
```

### 5. Story Management Flow

```
┌──────────────────┐
│ Dashboard        │
│ List of Stories  │
└──────┬───────────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
┌──────┐ ┌──────────────┐
│View  │ │Edit/Delete   │
│Story │ │Story         │
└──┬───┘ └──────┬───────┘
   │            │
   │            │ Delete
   │            ▼
   │      ┌──────────────┐
   │      │Confirm Delete│
   │      └──────┬───────┘
   │             │
   │             │ Confirm
   │             ▼
   │      ┌──────────────┐
   │      │DELETE /api/  │
   │      │stories/{id}/ │
   │      └──────┬───────┘
   │             │
   │             │ Success
   │             ▼
   │      ┌──────────────┐
   │      │Remove from   │
   │      │List          │
   │      └──────────────┘
   │
   │ View
   ▼
┌──────────────────┐
│ Story Player     │
│ (Read-only view) │
└──────────────────┘
```

## Superadmin Flow

### 1. Admin Dashboard

```
┌──────────────────┐
│ /admin/          │
│ Django Admin     │
└──────┬───────────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
┌──────┐ ┌──────────────┐
│Users │ │Stories        │
│Mgmt  │ │Management     │
└──┬───┘ └──────┬───────┘
   │            │
   │            │ View All Stories
   │            ▼
   │      ┌──────────────┐
   │      │Story List    │
   │      │(All Users)   │
   │      └──────┬───────┘
   │             │
   │             │ Filter by User
   │             │ Search by Title
   │             │ View Details
   │             ▼
   │      ┌──────────────┐
   │      │Story Details │
   │      │(Read-only)   │
   │      └──────────────┘
   │
   │ User Management
   ▼
┌──────────────────┐
│ User List        │
│ - Search         │
│ - Filter         │
│ - Actions:       │
│   • Edit         │
│   • Activate     │
│   • Deactivate   │
│   • Delete       │
└──────────────────┘
```

## Data Flow Diagrams

### Story Creation Data Flow

```
Frontend                    Backend                    AWS Bedrock
   │                           │                           │
   │ POST /api/stories/        │                           │
   ├──────────────────────────>│                           │
   │                           │                           │
   │                           │ Save to DB                │
   │                           │ (Story record)            │
   │                           │                           │
   │                           │ If image exists:          │
   │                           │ Analyze Image             │
   │                           ├──────────────────────────>│
   │                           │                           │ Titan Embeddings
   │                           │<──────────────────────────┤
   │                           │ image_description         │
   │                           │                           │
   │                           │ Generate Story            │
   │                           ├──────────────────────────>│
   │                           │                           │ Nova 2 Lite
   │                           │<──────────────────────────┤
   │                           │ story_text                │
   │                           │                           │
   │                           │ Synthesize Speech         │
   │                           ├──────────────────────────>│
   │                           │                           │ Nova 2 Sonic
   │                           │<──────────────────────────┤
   │                           │ audio_data                │
   │                           │                           │
   │                           │ Save audio file           │
   │                           │ Update story record       │
   │                           │                           │
   │<──────────────────────────┤                           │
   │ Story data + audio URL    │                           │
   │                           │                           │
```

### Story Playback Data Flow

```
Frontend                    Backend                    Storage
   │                           │                           │
   │ GET /api/stories/{id}/    │                           │
   ├──────────────────────────>│                           │
   │                           │ Fetch from DB             │
   │                           │                           │
   │<──────────────────────────┤                           │
   │ Story data                │                           │
   │                           │                           │
   │ GET /media/stories/       │                           │
   │ audio/{filename}          │                           │
   ├──────────────────────────>│                           │
   │                           │                           │
   │                           │ Read audio file           │
   │                           ├──────────────────────────>│
   │                           │                           │
   │                           │<──────────────────────────┤
   │                           │ audio bytes               │
   │                           │                           │
   │<──────────────────────────┤                           │
   │ Audio stream               │                           │
   │                           │                           │
   │ Play audio in browser      │                           │
   │                           │                           │
```

## State Management

### Frontend State (React Context)

```javascript
{
  auth: {
    user: {
      id: number,
      email: string,
      username: string,
      role: string,
      token: string
    },
    isAuthenticated: boolean
  },
  stories: {
    list: Story[],
    current: Story | null,
    loading: boolean,
    error: string | null
  },
  player: {
    isPlaying: boolean,
    currentTime: number,
    duration: number,
    volume: number
  }
}
```

### Backend State (Database)

```python
Story {
  id: UUID
  user: ForeignKey(User)
  title: string
  prompt: string
  story_text: string
  template: string
  image: FilePath (optional)
  image_description: string (optional)
  audio_file: FilePath
  is_published: boolean
  created_at: datetime
  updated_at: datetime
}
```

## Error Handling Flow

```
┌──────────────────┐
│ User Action      │
└──────┬───────────┘
       │
       │ API Call
       ▼
┌──────────────────┐
│ API Endpoint     │
└──────┬───────────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
┌──────┐ ┌──────────────┐
│Success│ │Error         │
└───┬───┘ └──────┬───────┘
    │            │
    │            │ Error Types:
    │            │ - Validation Error (400)
    │            │ - Authentication Error (401)
    │            │ - Not Found (404)
    │            │ - Server Error (500)
    │            │ - AWS Error (503)
    │            ▼
    │      ┌──────────────┐
    │      │Error Response│
    │      │{              │
    │      │  "error": "...",│
    │      │  "details": {...}│
    │      │}              │
    │      └──────┬───────┘
    │             │
    │             ▼
    │      ┌──────────────┐
    │      │Frontend      │
    │      │Error Display │
    │      │(Toast/Alert) │
    │      └──────────────┘
    │
    │ Success
    ▼
┌──────────────────┐
│Update UI         │
│Show Success      │
│Message           │
└──────────────────┘
```

## Key User Interactions

1. **Voice Input**: Click microphone → Speak → Convert to text → Use as prompt
2. **Image Upload**: Drag & drop or click to upload → Preview → Analyze → Use in story
3. **Story Playback**: Click play → Audio streams → Text highlights → Can pause/resume
4. **Story Modification**: Click "Modify" → Enter changes → Regenerate → New audio
5. **Story Saving**: Auto-save on creation → Manual save option → Saved to "My Stories"

